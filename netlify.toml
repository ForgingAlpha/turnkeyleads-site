[build]
  publish = "dist"
  command = "npm run build"
[build.processing]
  skip_processing = false
[build.processing.html]
  pretty_urls = false
[build.processing.forms]
  parse = true
# Headers are now defined in public/_headers
